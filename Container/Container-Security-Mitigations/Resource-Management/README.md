## Resource Management


**Note:**
```commandline
--storage-opt           Storage driver
--cpus		        Number of CPUs
-m                      Memory Size 

```

* Run `docker run -it --storage-opt size=10G -m=4M --cpus=".5" ubuntu:latest /bin/bash`